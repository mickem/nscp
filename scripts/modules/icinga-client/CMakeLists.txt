cmake_minimum_required(VERSION 2.6)

SET(TARGET icinga-client)

PROJECT(${TARGET})

CREATE_ZIP_MODULE(${TARGET} ${CMAKE_CURRENT_SOURCE_DIR})

ADD_CUSTOM_TARGET(${TARGET} ALL DEPENDS ${BUILD_TARGET_LIB_PATH}/${TARGET}.zip)
