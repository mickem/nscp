local target = installer_dll ;
local source = main_dll ;

lib $(target)
	: # sources
	$(source).cpp
	../../include/ServiceCmd.cpp
	/psdk//Msi
	/psdk//bufferoverflowu
	: # requirements
	<def-file>$(source).def
	<user-interface>gui
	<runtime-link>static
	<threading>multi
	: # default build
	: # usage requirements
	;
