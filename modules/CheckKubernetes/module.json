{
	"module"		: {
		"title"			: "Kubernetes cluster checker",
		"description"	: "Check a Kubernetes cluster through its API server: cluster health, pods, nodes and workloads.",
		"name"			: "CheckKubernetes",
		"alias"			: "kubernetes",
		"version"		: "auto",
		"experimental"	: true
	},

	"settings"		: {
		"default_alias"	: "kubernetes"
	},

	"commands" : {
		"check_kubernetes"	: "Check that the Kubernetes API server is reachable and ready: version, readiness and node counts.",
		"check_pods"		: "Check the state of pods: phase, the kubectl STATUS column, readiness and restarts.",
		"check_nodes"		: "Check node readiness, pressure conditions, schedulability and capacity.",
		"check_workloads"	: "Check that deployments, statefulsets and daemonsets have their desired replicas available."
	}
}
