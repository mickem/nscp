cmake_minimum_required(VERSION 2.6)

SET(TARGET CheckTaskSched2)
	
PROJECT(${TARGET})

CREATE_MODULE(SRCS ${CMAKE_CURRENT_SOURCE_DIR} ${CMAKE_CURRENT_BINARY_DIR})

SET(SRCS ${SRCS}
	stdafx.cpp
	"${TARGET}.cpp"
	TaskSched.cpp
	filter.cpp

	${NSCP_DEF_PLUGIN_CPP}
	${NSCP_FILTER_CPP}
)

ADD_DEFINITIONS(${NSCP_GLOBAL_DEFINES})

SET(SRCS ${SRCS}
	stdafx.h
	"${TARGET}.h"
	TaskSched.h
	filter.hpp	

	${NSCP_DEF_PLUGIN_HPP}
	${NSCP_FILTER_HPP}
)
IF(NOT MSVC11)
INCLUDE_DIRECTORIES(${PSDK_61_INCLUDE_DIRS})
LINK_DIRECTORIES(${PSDK_61_LIBRARY_DIRS})
ENDIF(NOT MSVC11)

add_library(${TARGET} MODULE ${SRCS})

target_link_libraries(${TARGET}
	${Boost_FILESYSTEM_LIBRARY}
	${NSCP_DEF_PLUGIN_LIB}
	${NSCP_FILTER_LIB}
	${Boost_DATE_TIME_LIBRARY}
	Mstask.lib
)

INCLUDE(${BUILD_CMAKE_FOLDER}/module.cmake)
#LINK_DIRECTORIES("${NSCP_LINK_DIRECTORIES}")
