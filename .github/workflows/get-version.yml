name: Fetch version

on:
  workflow_call: {}


jobs:
  build:
    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v3
      with:
        submodules: true
        fetch-depth: 0

    - name: Fetch version from git tags
      id: version
      uses: codacy/git-version@2.7.2
      with:
        release-branch: main

    outputs:
      version: ${{ steps.version.outputs.version }}